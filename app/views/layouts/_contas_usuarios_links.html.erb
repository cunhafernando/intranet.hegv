<% if user_signed_in? %>
    <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.first_name %> <%= current_user.last_name %>
        </button>
        <ul class="dropdown-menu">
            <li>
                <%= link_to 'Editar usuário', edit_user_registration_path, class: 'dropdown-item' %>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
                <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            </li>
        </ul>
    </div>
<% else %>
    <%= link_to 'Entrar', new_user_session_path, class:" py-2 mb-2 btn btn-outline-primary rounded-3" %>&nbsp;&nbsp;
    <!--<%= link_to 'Resgitre-se', new_user_registration_path, class:" py-2 mb-2 btn btn-outline-primary rounded-3" %>-->
<% end %>