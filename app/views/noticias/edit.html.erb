
  <div class="row align-items-center mt-5">
      <div class="col-3">
      
      </div>
      <div class="col-6">
        <h1>Editar Notícia</h1>
      </div>
      <div class="col-3">
      
      </div>
  </div>
  <%= render "form", noticia: @noticia %>

  <br>

    <%= link_to "Voltar", noticias_path, class:"btn btn-outline-success mb-3" %>
    <%= link_to "Mostrar", @noticia, class:"btn btn-outline-primary mb-3" %>
    <%= button_to "Apagar", @noticia, method: :delete, class:"btn btn-outline-danger mb-3" if policy(@noticia).update? %>
  </div>
