<%= simple_form_for [@noticia, @noticia.comentarios.build] do |f| %>
    <div class="form-inputs">
        <%= f.input :body, label: 'Escreva seu comentário', input_html: {rows: 10} %>
    </div>
    <div class="forma-action">
        <%= f.button :submit, class: 'btn btn-primary mb-3' %>
    </div>
<% end %>