
  
  <div class="row mb-2 mt-4">
    <% if notice.present? %>
        <div class="alert alert-success" role="alert">
            <%= notice %>
        </div>
    <% end %>

    <% if alert.present? %>
        <div class="alert alert-danger" role="alert">
            <%= alert %>
        </div>
    <% end %>
  </div>

  <div class="row g-5">
    <div class="col-md-12">
      <h3 class="pb-4 mb-4 fst-italic border-bottom">
        Notícia
      </h3>

      <p style="color: green"><%= notice %></p>
      <%= render @categoria %>

      <div class="mb-4">
        <%= link_to "Editar", edit_categoria_path(@categoria), class:"btn btn-outline-secondary rounded-pill mb-3" %>
        <%= link_to "Voltar", categorias_path, class:"btn btn-outline-primary rounded-pill mb-3" %>
      </div>
      <div class="mb-4">
        <%= button_to "Apagar", @categoria, method: :delete, class:"btn btn-outline-danger rounded-pill mb-3" %>
      </div>

      

    </div>
  </div>

